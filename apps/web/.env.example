# Base URL of the NestJS API
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# Dev-only: the API key is exposed to the browser in this test client.
# Do NOT ship a real production key this way.
NEXT_PUBLIC_API_KEY=change-me-dev-key
